# Research Goal Examples

Store reusable case inputs here.

Each YAML file can reference:

- `user_files`
- `experimental_files`
- `literature_sources`
- `user_constraints`

This makes it easier to rerun the same study configuration across many materials.
